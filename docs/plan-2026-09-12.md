# Castor Research: diagnosis and plan, 2026-09-12

Produced by an adversarially verified audit (eight subsystem readers, one skeptic per reader, a synthesizer, a completeness critic) plus one measured real run (`tools.run_live diag01`). Every finding below was confirmed against the files by a skeptic instructed to refute; two were refuted and are listed at the end. Items carry the finding they close, an effort (S under an hour, M a day, L more), a fix, and the test that fails without it.

Counts: 50 raised, 48 confirmed, 2 refuted, 24 extra items from skeptics.

## Measured baseline: one real run (stub off, free backends)

757s wall. Tools 466s (62%): 792 calls overlapping about 1.6x; validate_domain x365 (197s), wayback_activity x22 (193s), web_search x37 (189s), probe_domain_patterns x37 (152s, 13 skeletons), resolve_brand_domain x23 (124s, 15 skeletons), poi_competition x1 (40s, an Overpass timeout recorded ok=True). Model calls 39 timed (126s), 53 total, 48.5k tokens in, $0. Zero cache hits. Zero of 828 tool entries recorded ok=False despite logged timeouts. TAM triangulated from one independent source. audiences, personas, pricing_benchmark dropped with reasons; 12 advisory findings; publishable.

## Findings by severity

### S1

- **RR-1** [reliability] Boot recovery's 60s grace window strands a run interrupted within a minute of its last checkpoint: never resumed, never refunded, and after 24h never even errored (`jobs.py:303`)
  - why: A deploy is the commonest way a run dies, and the checkpoint cadence puts a large share of run time inside the 60s window. A paying founder then has a spent credit, a report that says it is still generating, and an account locked out of starting another for an hour; the refund logic that was added for exactly this case is unreachable. Verified empirically in the venv against a scratch JOBS_DB_PATH
  - proof: A test that creates a plan job, sets state='running' with updated_at = now-15 and a checkpointed result, runs the lifespan's _resume_interrupted_runs, and asserts the job is in pending_ids (or resumed) fails today with state still 'running'.

- **M1** [billing] A refunded run keeps its included revision (a free full run) and its JSON artifact (`routes/research.py:317`)
  - why: Reproduced against a temp DB with real Stripe env shape (keys set, PAYWALL_OFF unset): buy 2 credits, two paid runs come back withheld and are refunded (balance 2 -> 2), then POST /jobs/{A}/revise answers 200 and starts a new six-minute run with 0 credits consumed and 0 daily runs counted; POST /plan {previous_job_id: B, description: an unrelated venture} also answers 200 free; GET /jobs/{A} serve
  - proof: A test that records a spend for a job, refunds it with billing.refund_for_job, then asserts POST /jobs/{id}/revise and POST /plan {previous_job_id: id} both answer 402 and start no job, and that GET /jobs/{id} carries no `result` while blocking findings stand; today all three answer 200.

- **M2** [billing] The only Stripe webhook points at the marketing site root; removing PAYWALL_OFF makes checkout charge cards and credit nothing (`api.py:1197`)
  - why: The read-only Stripe check also shows the five most recent checkout sessions (2026-09-03 to 2026-09-05) all created against quick-tunnel success URLs and expired unpaid, and the newest fulfilled entitlement rows in the live DB date from 2026-09-03; nothing has ever been fulfilled through the stable host. The day PAYWALL_OFF is removed, a real (test-mode today, live-mode later) card is charged 29/9
  - proof: Complete a test-mode checkout started from app.castor-advisory.com with card 4242 and poll GET /billing/status for 60 seconds: report_credits stays 0 today. Unit form: a boot check asserting some registered webhook URL equals CASTOR_PUBLIC_URL + '/billing/webhook' fails today.

- **OPS-1** [ops] The only copy of every account and paid entitlement has no backup, scheduled or otherwise (`/Users/charlieyan/Downloads/castor-advisories/backup.py:15`)
  - why: A single SSD holds 11 accounts, 7 entitlements, 2 credit spends and 6 coupons. Losing it loses customers' purchases with no way to reconstruct who bought what, and (per backup.py:6-7) no verified way to tell them.
  - proof: A test that asserts a castor-*.sqlite newer than 24h exists in the configured backup directory and that `backup.py --verify` on it reports every table matching would fail today because no such directory or file exists.

- **OPS-2** [ops] The production boot guard is one check; a paywalled instance with CASTOR_STUB_REPORT set charges a credit and delivers a test clone (`/Users/charlieyan/Downloads/castor-advisories/api.py:349`)
  - why: Answering 'what must be set before CASTOR_ENV=production': the guard enforces exactly SESSION_SECRET. Everything else production changes is behavioural (secure cookies api.py:299/520/765, 401 for anonymous :110-112, https base :1150, /feedback/stats hidden :1269) and every test switch stays honoured. The known state says the stub is on and paywall is off; the day one of those flips without the oth
  - proof: A test that sets CASTOR_ENV=production, SESSION_SECRET, and CASTOR_STUB_REPORT to a finished job id, then enters the lifespan and expects RuntimeError, fails today because the lifespan yields.


### S2

- **RR-2** [reliability] A timed-out or errored market sizing, 4Ps, PSM or place reaches the buyer as a plausible empty: no dropped-output reason, the template hides the section, D54 passes, nothing withholds or refunds (`plan.py:2038`)
  - why: This is the 'wrong result that looks like a right one' the audit asks about. The assembler's disclosure guarantee covers four sections; the four most expensive numeric steps are joined through run_labeled, whose timeout contract is to return {'error': 'timeout after Ns'}, and every call site turns that into a log line. The buyer sees a report with no market size and no 4Ps, an explanation pointing
  - proof: A test that patches _sizing_task (or the LLM) to sleep past 90s inside run_sizing_stage and asserts result['_dropped_outputs']['market_sizing'] names the timeout fails today with KeyError.

- **RR-3** [reliability] The owner's intermittent missed run is explainable: hard join timeouts against a Gemini-only chain, plus timed-out threads that keep running and keep reserving the rate gate for the next step (`capabilities/scheduler.py:66`)
  - why: 'A section sometimes misses its run' is what a 90s deadline against a throttled single provider looks like from the outside: the step ran, was still running when the batch stopped waiting, and its result was discarded. Because the orphan keeps consuming the shared 4s-per-call reservation, the next expensive step starts late and is likelier to time out too, so misses cluster within one run. Combine
  - proof: A test that stubs _try_one_backend to sleep 5s and return None for the first three chain attempts, runs run_sizing_stage with the real 90s join and asserts result['_llm_exhaustion'] or a dropped-output reason mentions the timeout fails today: the result carries neither.

- **G1** [grounding] Source tiers exist on a code path production never takes; a content mill and an SEC filing get one vote each (`source_tiers.py:164`)
  - why: The whole tiering apparatus (roster of 1,247 harvested domains, sigma table, docstring promising 'the one place that decision is allowed to reach the arithmetic') has no effect on any number a buyer reads. Under the shipping rule an origin citing deepmarketinsights.com and one citing sec.gov produce the identical headline; measured with a read-only snippet: both give mid=600,000,000 under the defa
  - proof: A test that runs market_sizing.apply_tam_triangulation on two origin-distinct methods, one sourced to sec.gov and one to deepmarketinsights.com, and asserts the headline differs from the run with both sourced to sec.gov fails today: both return mid=600,000,000.

- **G3** [grounding] Every gate is the report checking itself; the formula gate tolerates 2.5x; the LLM layer is off; baselines are write-only (`report/verifier.py:230`)
  - why: Answer to 'which gates could pass on a report whose numbers are wrong': all 61, by construction, for any venture whose sizing method is not trade_area_catchment (every one of the 17 benchmark cases and every B2B/national report). A TAM that is 10x wrong but arithmetically tidy, with a SAM slice that matches, a SOM under 40% and a formula within 2.5x, produces status=VERIFIED, publishable=True, a g
  - proof: A test that builds a figure with value_usd=1,000,000 and calc='1200000 * 2' and asserts a BLOCK finding fails today (returns no finding); a test asserting gates.run_gate can be pointed at docs/baselines/M3.json and report a regression fails because no such argument or reader exists.

- **G4** [grounding] A credit is consumed before any content check; a 30-character junk brief with no intake starts a paid, non-refunded run (`routes/research.py:80`)
  - why: Answer to 'does the intake let a run start on an empty or nonsensical record and spend a credit': yes. curl with 30 x's, or a survey session whose extractor returned nothing, reaches billing.consume before company_profile even runs; the profile step then fills every field with 'unknown', the pipeline sizes an 'unknown' category, the verifier judges the result self-consistent, and the credit is not
  - proof: A test posting {description: 'x'*30} for an owner holding one credit and asserting HTTP 422 and an unchanged credit balance fails today: the request validates, billing.consume runs, and the job is created.

- **SEC-1** [security] Public host serves login and cookies over plaintext http with no Secure flag and no HSTS; CASTOR_ENV unset also opens /feedback/stats cross-tenant (`api.py:517`)
  - why: Live probes: GET http://app.castor-advisory.com/auth/me answered HTTP/1.1 200 over plaintext (no redirect) with Set-Cookie castor_guest ... HttpOnly; SameSite=lax and no Secure; http://app.castor-advisory.com/login answered 200 with the 14.8KB login form, whose relative POST /auth/login would carry the password in clear; https://app.castor-advisory.com/ carries no strict-transport-security or any 
  - proof: A test that boots the app with CASTOR_ENV unset, logs in, and asserts the session Set-Cookie carries Secure and the response carries Strict-Transport-Security fails today; a curl of http://app.castor-advisory.com/login expecting a 301 to https gets 200.

- **SEC-2** [security] DELETE /auth/account returns success but leaves the verbatim venture transcript, reader notes, feedback, library title and coupons behind (`auth.py:448`)
  - why: The endpoint proves the password, returns ok with per-table counts, and looks like a complete erasure, while the founder's own words (venture description in the transcript, margin notes and questions in iteration, feedback comments) and their public library title remain readable on disk and in the database. A wrong result that looks right, on the one flow the product cites a legal obligation for. 
  - proof: A test that creates an account and a job, adds an annotation via iteration.add_annotation, a feedback row, and writes a transcript file, then deletes the account and asserts the iteration row, feedback row and transcript file are gone, fails today.

- **M3** [billing] A paid run interrupted past MAX_RESUMES, or stranded longer than 24 hours, is buried with the credit kept (`jobs.py:258`)
  - why: The ledger was added precisely so a run interrupted by a deploy is still 'somebody's paid run' (research.py:265-271), but the two terminal branches that bury a run bypass it: three restarts inside six minutes (a crash loop, which launchd's KeepAlive/ThrottleInterval 5s makes easy) or a Mac asleep for a day leaves state=error or a permanent 'running' with credit_spends.refunded_at NULL and the foun
  - proof: A test that record_spend()s a job, sets it running with params._resumes=2 and updated_at 2 minutes old (and a second with updated_at 2 days old), calls jobs.requeue_orphans() and asserts billing.was_refunded(job) and the balance restored; today both stay unrefunded.

- **UX-1** [product] The public library page renders an empty shelf for every visitor: its script runs before the transport it calls is loaded (`web/library.html:197`)
  - why: Verified in a real browser against a scratchpad DB seeded with one published report: the shelf is blank, no card and no 'Nothing has been published yet' text, and the console shows 'Uncaught TypeError: Cannot read properties of undefined (reading reason) at /library:230'. The live database holds 5 non-withdrawn shared_reports rows, so five founders published work that no stranger can see, and the 
  - proof: A test that loads /library in a JS runtime with one entry in /library.json and asserts a .vol element exists would fail today with a TypeError and pass after the script order is fixed.

- **UX-2** [product] Withheld page's 'Answer & rerun' starts a new run, then sends the founder to /progress.html?job=undefined (`routes/jobs.py:113`)
  - why: TestClient confirmed the served withheld page contains encodeURIComponent(r.job_id), and the browser confirmed /progress.html?job=undefined renders 'This run stopped early'. So the one repair path the product offers on a withheld report spends the founder's run (and, once the paywall is on, their credit) and then tells them it failed; the new report only surfaces later in the library with no link 
  - proof: A test rendering _withheld_page with one remedy and asserting the script navigates with the parsed body's job_id (assertNotIn('r.job_id') / assertIn('.job_id') on a parsed variable) fails today; no test currently covers _remedy_form_html at all (grep found none).

- **CH-1** [codehealth] A crashed scale classification ships a physical venture with national digital sizing marked publishable, and every gate abstains (`plan.py:2461`)
  - why: An LLM classifier call (skills/sizing/classify.py:147 uses call_json) can raise on backend exhaustion or a parse failure; when it does, the report that follows is indistinguishable from a healthy digital venture's. This is the 'gate abstention has two meanings' failure at the sizing root: wrong-shape and data-missing both read as N/A.
  - proof: Patch skills.sizing.classify.classify_market_scale to raise, run run_sizing_stage on a cafe brief with estimate_market_size stubbed to a digital TAM, and assert result['market_sizing']['publishable'] is False and D52 is not None; today publishable stays True and D52 abstains.


### S3

- **RR-4** [reliability] resume_interrupted runs a paid job through a copy of the worker that skips _finish: a resumed revision never settles, never answers its carried questions, loses its delta link and refine, and runs the real pipeline under the stub (`routes/research.py:275`)
  - why: The docstring at routes/research.py:265-272 says a resumed run 'is still somebody's paid run' and fixed the refund and the mail, but the product surface a revision buys (answers to the reader's questions and a final stamp) is only produced by the code path a deploy takes away. The stub interaction is a cost surprise: the one time real API money is spent on this instance today is when a stale pendi
  - proof: A test that requeues an interrupted plan job whose params carry previous_job_id and unanswered carried questions, runs resume_interrupted with run_plan stubbed, and asserts iteration.get_state(job_id)['status'] == 'final' fails today.

- **G5** [grounding] No case, fixture or corpus report carries a known outcome; the benchmark's TAM band is 6x-50x wide under a copy-pasted key (`benchmarks/score.py:94`)
  - why: Answer to 'what would it take to score one report against a known outcome': there is nothing to score against. Every reference is an input-side fact (funding, analyst band, named competitors) or a band so wide that a TAM 10x from the midpoint scores 100. The approximation-by-accumulation hypothesis is therefore untested by construction, and the one number a founder acts on (SOM / Y1 revenue) has n
  - proof: A test asserting score_tam(result with tam.mid=$10B, refs 0.5B/3B/10B)['score'] < 100 fails today (returns 100); a test asserting any case file contains a realized-outcome field fails because the schema has none.

- **G6** [grounding] The question agent sees a lossy digest; the revision agent never sees the report it is revising (`iteration.py:269`)
  - why: Answers the open design question. The question agent now gets every section key but a shape-shrunk copy: a 40-row competitor roster becomes 40 then 24 then 14 rows and each narrative is cut to 1,500 then 900 characters, so 'why is competitor #31 ranked above #12' or a question about a paragraph past the cut is answered from a report that no longer contains it, with grounded=true. The revision agen
  - proof: A test calling post_revise on a finished job with a stubbed run_plan and asserting the stub received the prior result (or any section of it) fails today: run_plan receives only the amended description, geo, weights, effort and intake.

- **SEC-4** [security] Under the documented proxy configuration every IP-keyed limiter and the guest daily cap key on a client-chosen X-Forwarded-For entry (latent; not the current Mac deployment) (`api.py:203`)
  - why: Cloudflare appends the connecting address to a client-supplied X-Forwarded-For, so the leftmost entry is whatever the caller wrote. On the Docker/Render/Fly path, or the moment someone follows .env.example, a stranger sends a fresh header per request and gets unlimited signups (each with three free runs a day), unlimited password guesses and unlimited guest runs. Today's launchd deployment is unaf
  - proof: With CASTOR_TRUST_PROXY=1, a test posting eleven wrong logins with a different X-Forwarded-For each time and expecting the eleventh to be refused fails today (the limiter never trips).

- **M4** [billing] Removing CASTOR_PAYWALL_OFF today turns 'three free reports a day, no card' into a 402 while the meter still says 3 (`routes/research.py:552`)
  - why: The paywall rule lives in one place on the server (research.py:494-508) but the product's promise lives in three others, and they disagree the moment the switch is flipped: a visitor signs up on 'no card', sees '3 free runs left', and is charged or refused at the survey. Whichever rule the operator wants, the copy and the meter must be derived from it.
  - proof: With STRIPE keys set and PAYWALL_OFF unset, GET /billing/status for a fresh guest returns needs_purchase=true and free_runs_left=3; a test asserting free_runs_left == 0 whenever needs_purchase is true (or that POST /plan succeeds free_runs_left times) fails today.

- **M5** [billing] Six founders hold $10 codes Stripe rejects; the boot push fails every code, leaks a coupon object per boot, and logs success (`/tmp/castor_server.log:5`)
  - why: The live DB shows all six coupon rows with stripe_promo_id NULL; every boot of the launchd server retries all six, creates six more orphan coupon objects in the Stripe account, fails, and reports 'pushed 6'. The reward the library trade depends on has never been payable, and the log says it is fixed.
  - proof: A test that stubs Stripe to accept only [A-Za-z0-9] codes and asserts create_promo_code(sharing._new_code(), 1000) succeeds; and a test that sync_pending() returns 0 when every push fails (today it returns 6).

- **UX-3** [product] Landing contact form says 'Message received' and sends nothing anywhere (`web/landing.html:986`)
  - why: Verified in the browser: after submitting the form the modal shows 'Message received.' and the network log shows no request left the page. On a phone, a locked-down corporate browser, or any machine without a mail handler, the consulting and corporate inquiries the page sells at $150/hour are silently dropped while the visitor is told they were received.
  - proof: A browser test that submits the contact form with mailto: navigation blocked and asserts either an outgoing request or the absence of 'Message received' would fail today.

- **UX-4** [product] Google sign-in button is visible and dead-ends on Google's error page; the login page's Google error copy can never fire for this failure (`web/login.html:148`)
  - why: TestClient: GET /auth/google is a 302 to accounts.google.com whose redirect_uri is https://app.castor-advisory.com/auth/google/callback. Because that URI is not registered on the OAuth client, Google renders 'Error 400: redirect_uri_mismatch' on its own domain and never redirects back, so none of the login page's four error messages applies; the visitor is stranded on a Google page and the only wa
  - proof: Cannot be unit-tested (the failure is on Google's side); a smoke test against the live host that follows the 302 and asserts the resulting page is not a Google 400 would fail today.

- **UX-5** [product] A failed run shows the founder the raw Python exception, while the library list drops the reason entirely (`jobs.py:519`)
  - why: Browser: the progress page for a seeded errored job shows 'NameError: name checkpoint is not defined' as the explanation; the live DB has 39 state='error' rows and that exact NameError is one of the most recent. TestClient: the /jobs row for the same job carries error: None, so the dashboard's 'Did not finish' row has no subtitle and the reader must click through to learn anything. A founder is to
  - proof: A test seeding state='error' with error='NameError: ...' and asserting (a) GET /jobs returns a non-empty error on that row and (b) GET report.html does not contain 'NameError' fails today on both counts.

- **UX-6** [product] Survey trap: Back from the reading stage returns to the prose box, and Continue from there silently starts a second interview and orphans the first; validation errors are never announced (`web/survey.js:1241`)
  - why: A founder who presses Back to add a sentence to their description and then Continue loses the reading-stage answers they already gave (they stay on the old session, which is now a duplicate draft), pays a second extraction call, and re-answers everything. Refresh at stages 3 or 4 also drops them back to stage 2 (survey.js:1333), which is safe but surprising. Screen-reader users never hear the vali
  - proof: A jsdom test that reaches stage 2, presses Back, presses Continue with unchanged prose, and asserts /intake/start was called exactly once fails today (it is called twice).

- **LC-2** [performance] Every hyperlocal run computes the full digital sizing (6 model calls, a price scrape, a Census fetch) and then replaces it (`plan.py:2038`)
  - why: 71 of 260 real runs (27%) ended with method=trade_area_catchment. In job 2849bbeb (hyperlocal) economics finished at 906s, six ~700-token sizing calls landed 929-971s, then the trade-area model replaced them and market_sizing was recorded at 1042s: roughly 60-95s of wall time and 6 of the run's ~44 rate-gated calls (13-15% of the 15 RPM budget) bought numbers that never reach the report, on the fr
  - proof: A test with scale=hyperlocal and a geocodable site (tools stubbed) asserting that market_sizing.estimate_market_size is never called fails today.

- **CH-3** [codehealth] Broad excepts: five bare `pass` blocks and six money/mail catches on the delivery path should be narrowed or logged; the rest are correct boundaries (`routes/research.py:409`)
  - why: Counted: 343 non-test broad excepts (355 with tests). A sample of 40 classifies as: 9 silent-pass, 9 log-and-drop step guards with no record on the report (CH-1), 6 money/mail catches that log and never retry, 10 correct boundaries (run_async, per-section, per-detector, BaseException-then-reraise, Stripe I/O, auth fail-closed), 6 identity/boot guards that are fine. The silent and money classes are
  - proof: Patch report.verifier.blocking_findings to raise, run _notify_owner on a result with blocking findings, and assert mailer.send_report_withheld was called; today send_report_ready is called.

- **OPS-3** [ops] A second, unrelated LaunchAgent supervises the same cloudflared tunnel and owns the only caffeinate; Castor's plists survive neither sleep, logout, nor that script (`/Users/charlieyan/Downloads/castor-advisories/config/launchd/com.castor.tunnel.plist:12`)
  - why: app.castor-advisory.com goes dark for the seconds it takes two supervisors to fight, every time the other project's hostname blips; idle-sleep protection depends on a script Castor does not own; lid-close (MacBookPro17,1) and logout stop the service regardless. Network changes are fine (cloudflared reconnects, KeepAlive restarts exits).
  - proof: A test that, with :8001 healthy and the other STABLE_URL returning non-200 twice, samples `pgrep -f 'tunnel run shuttle'` over 15 seconds and expects a constant count of 1 would observe 1 -> 0 -> 2.


### S4

- **RR-5** [reliability] The ledger's step label is a ContextVar that does not reach thread-pool workers, so every LLM and tool call made inside a fan-out is attributed to no step; a step with zero attributed calls in the provenance panel reads as one that never ran (`persistence/ledger.py:15`)
  - why: Proved in the venv: with ledger.set_step('pricing') on the main thread, a ThreadPoolExecutor worker's ledger.current_step() returns None. Every fan-out call is filed under no step, so the panel shows the expensive steps with no calls under them. That is a plausible second source of the 'agent missed its run' report and it makes the transcript unable to answer which step spent the money.
  - proof: A test that enters step_scope('pricing'), submits ledger.current_step to a ThreadPoolExecutor and asserts the worker returns 'pricing' fails today with None.

- **SEC-3** [security] Anonymous, unmetered LLM spend: POST /intake/start makes one extraction call per request with no quota; regenerate and iterate are unlimited per job (`routes/intake.py:66`)
  - why: A loop against /intake/start burns the paid model key (LLM_ALLOW_PAID) with no ceiling and fills intake_sessions with a row per call; an owner of one report can loop regenerate or iterate indefinitely. Pure cost, no data exposure.
  - proof: A test that posts /intake/start with initial_message 50 times from one client and expects a 429 before the 50th fails today (all 200, 50 model calls).

- **SEC-5** [security] Operator and developer surfaces answer on the public host: /usage, /api/tools|skills|agents, /architecture, /benchmarks (`routes/pages.py:281`)
  - why: No customer data, but the operator's model bill and the full internal capability map are readable by anyone, and the endpoints have no product purpose on a paid public site.
  - proof: A test under CASTOR_ENV=production asserting GET /usage and GET /api/tools return 404 fails today.

- **SEC-6** [security] Repo hygiene: feedback-for-jane.pdf/.txt still tracked; history carries no key material; .env never tracked (`feedback-for-jane.txt:1`)
  - why: A personal document about a named person sits in a repo that is shared with agents and worktrees and may be published; everything secret-shaped is confirmed absent from history, so the pasted keys need rotating for the chat transcript's sake only, not the repo's.
  - proof: A hygiene test asserting git ls-files contains no path matching feedback-for-jane fails today.

- **M6** [billing] Two LLM endpoints are behind ownership only: no meter, no credit, no repeat cap (`routes/jobs.py:300`)
  - why: With PAYWALL_OFF=1 today every guest gets a finished report for free and can loop /regenerate or /iterate against the operator's model budget with no ceiling; with the paywall on, one $29 credit still buys unlimited regenerations. The docstring at quota.py:3-6 names POST /plan as the abuse surface; these two are the same surface at a smaller unit.
  - proof: A test that POSTs /jobs/{id}/regenerate _aux_daily_limit()+1 times in one day with the LLM stubbed and expects a 429 on the last call; today every call answers 200.

- **UX-7** [product] Copy on /login and the landing page disagree about price, and both are only true while CASTOR_PAYWALL_OFF=1; /home sells a guest a credit the switch makes unnecessary (`web/login.html:132`)
  - why: TestClient under the live .env: billing/status reports configured=true, every pack buyable, needs_purchase=false; the browser shows a guest on /home a working 'Buy a credit' button (Stripe test mode today, real Stripe once keys flip) for a credit that buys nothing they do not already have. The two entry pages make opposite promises (free vs $29) and neither is tied to the switch that decides which
  - proof: A test that renders /login with billing configured and paywall on and asserts the page does not promise free reports would fail today; a test that GETs /billing/status under CASTOR_PAYWALL_OFF=1 and asserts /home does not draw a Buy control would fail today.

- **LC-1** [performance] 4Ps join waits for a timed-out section's late answer, then ships the placeholder anyway (`four_ps.py:1174`)
  - why: 14 of 260 real runs shipped at least one '(section timed out)' 4Ps narrative, 5 of them all four sections. Trace of job 2849bbeb: four_ps started ~1042s, the product section's first answer landed at 1064.9s and failed the schema, the re-ask landed at 1151.3s (109s in, 2758 input tokens), and the four_ps step was recorded at 1151.4s: the pipeline waited the full 109s for the answer, paid for both c
  - proof: A test where a fake _run_section returns 0.3s after a 0.2s per-section timeout inside a 1s pool budget must find the section text in the assembled plan, not '(product section timed out)'; it fails today.

- **LC-3** [performance] One report generation per process: a second submitter's run sits pending for the whole first run (`jobs.py:436`)
  - why: The last 20 real runs took 363-1793s (median ~900s). With a single uvicorn process (~/Library/LaunchAgents/com.castor.server.plist launches `-m uvicorn api:app --host 127.0.0.1 --port 8765` with no --workers), two buyers submitting within that window means the second waits the entire first run before any step of theirs begins, and nothing in the job row distinguishes 'queued behind another run' fr
  - proof: A test that POSTs /plan for two different owners with a 2s stubbed run and asserts both rows read state='running' within 1s fails today (the second stays 'pending').

- **LC-4** [performance] The post-discover chain runs serially although four of its steps depend only on the evidence bundle (`plan.py:2405`)
  - why: Trace of job 6fe47140: personas 171.5s -> consumer_research 214.9s -> differentiators 286.9s -> max_diff 343.8s, 172s of wall time for four steps that write disjoint keys and read none of each other's output (58f0a4f7: 112s for the same stretch). Overlapping them, and starting the trade-area fetches at discover end, removes roughly 100-170s from a 530-590s run; the rate gate means it cannot remove
  - proof: A test that stubs the four step functions to record start/finish order and asserts all four start before any finishes fails today.

- **LC-5** [performance] The suite pays fixed sleeps that fire with sockets blocked or requests mocked, and its retry-disable switch is inert (`net.py:41`)
  - why: Sampled: test_infra.py 200 tests in 35.2s with 4 tests = 32.7s; test_run_plan_steps_extracted.py 81 in 6.7s; test_scraper_fixes.py 24 in 1.1s. Six of 305 tests carried 38 of 43 seconds, all in sleeps or a model load, and the remaining 299 averaged 17ms. The 8m38s suite is that long tail scaled to 4114 tests, plus a latent 15.6s tax on any test that reaches net.request without a mock. pytest-xdist 
  - proof: A test under the offline fixtures asserting net.get('https://www.trustpilot.com/x') returns in under 1s fails today (15.6s); a test asserting 13 mocked fetches of one host finish in under 1s fails today (6.3s).

- **LC-6** [performance] The COGS tracker records $0.00 for every run because the models actually used are not priced, and call_text bypasses the ledger (`llm.py:29`)
  - why: All 27 real runs that carry _cogs record usd 0.0; the models seen are gemini-flash-latest (27 runs) and gemini-flash-lite-latest (13). By the ledger a report is a median 44 calls, 37.5k input and 12.7k output tokens (172-378 tool calls, all free sources); at the table's own Haiku 4.5 rate that is ~$0.10 and at Sonnet 4.5 ~$0.30, and the panel would still say $0 the day a paid Gemini tier or a new 
  - proof: A test asserting every model id in BACKEND_DEFAULTS and _call_gemini's fallback list has a PRICING entry fails today.

- **CH-2** [codehealth] The four reassign_owner functions are one pattern, but sign-in runs them in an order that strands drafts and shares if any step raises (`jobs.py:113`)
  - why: In _set_session, if sharing.reassign_owner or intake.reassign_owner raises (locked DB, schema drift), the jobs and entitlements have already moved, the exception is swallowed at api.py:563, and the later /auth/verify retry in _claim_prepaid cannot find the guest because its entitlement rows are gone. The founder's drafts and published-report coupons stay under a cookie id nobody can present again,
  - proof: Sign up with a guest cookie holding one paid entitlement, one job and one intake draft while intake.reassign_owner is patched to raise once; then hit /auth/verify. The draft is still owned by the guest id and intake.drafts(account) is empty, because billing rows moved before the failure and guest_ow

- **CH-4** [codehealth] plan.py is five modules in one file and three lower modules reach sideways into it at call time; split it first (`plan.py:83`)
  - why: The three sideways imports are cycles worked around with deferred imports, the pattern the layering work just spent effort removing at the api/report boundary. run_plan at 430 lines with 33 broad excepts is where CH-1's silent drops live; nobody can test block 5 without importing blocks 1-4. 66 functions over 80 lines measured (scout said 62); plan.py holds 8 of them.
  - proof: Add `plan` to a 'facade-only' import check in test_the_layers_only_point_downward.py; it fails today at gates/pricing.py:271, orchestrator/steps/evidence.py:63 and preview.py:242, and passes after the extraction.

- **CH-5** [codehealth] A third of September's tests grep source text, and the web JS is never executed by anything (`test_the_gate_answers_every_click.py:47`)
  - why: Measured: 666 tests in the 59 files git added since 2026-09-01. In a 25-file sample (324 tests): 104 assert source text (32%), about 8 assert shape (2.5%), 212 assert behaviour (65%). The source-text tests do fail on a literal revert, so the base rate of a test passing against its own reverted fix is low (the three known cases are the ones documented above, all since repaired); the cost is the oth
  - proof: The test that would fail without the fix is the one that does not exist: a node harness that calls survey.js gate() with fetch stubbed to answer 402 and asserts the POST to /plan is never issued. Today changing gate()'s runtime behaviour while keeping its text breaks nothing.

- **CH-7** [codehealth] Routes duplicate the ownership seam: 11 unreachable `if not _owned_job` branches, 20 call-time `from api import`, and one route re-entering another (`api.py:210`)
  - why: The dead branches read as if the choke point can return None, which is the assumption that would make a future copy of _owned_job unsafe. Twenty scattered call-time imports are twenty places a refactor of api.py can break at request time rather than import time, with no test on the ones no test exercises.
  - proof: A coverage run over the routes tests shows zero hits on the 11 `raise` lines; deleting them changes no test outcome, which is what makes them worth deleting.

- **OPS-4** [ops] render.yaml and DEPLOY.md build branch master, which has no root Dockerfile; merging to master must reconcile 4 unmerged origin/master commits against renamed paths on git 2.15 (`/Users/charlieyan/Downloads/castor-advisories/render.yaml:16`)
  - why: Answering 'what a merge to master would carry': 681 files / +38,556 / -12,286 from the branch, plus 994 lines of competitor-pricing work that exists only on origin/master and would land inside a directory the branch deleted. Until then every container deploy path in the repo points at a tree that cannot build.
  - proof: `git show master:Dockerfile` (and origin/master:Dockerfile) errors with 'path does not exist', which is what Render's build step would hit.

- **OPS-5** [ops] The Docker image would bake in the session signing key and ~384MB of local state (`/Users/charlieyan/Downloads/castor-advisories/Dockerfile:35`)
  - why: Anyone with pull access to the image registry gets the key that mints sessions for every current account, and each deploy ships 384MB it never reads.
  - proof: A test that parses both ignore files and asserts .session_secret and data/ appear in .dockerignore fails today.

- **OPS-6** [ops] No access log, no log retention, no readiness check, no alerting; /healthz is a constant (`/Users/charlieyan/Downloads/castor-advisories/config/launchd/com.castor.server.plist:18`)
  - why: An outage is noticed by a customer or by the owner opening the site; a stuck worker or corrupted DB stays 'ok'; after a reboot there is nothing to read.
  - proof: A test that points JOBS_DB_PATH at an unopenable path and requests /healthz still receives 200 {ok: true}.

- **OPS-7** [ops] README, DEPLOY and SHIP describe a product that no longer exists; the live run path and secret rotation are documented nowhere (`/Users/charlieyan/Downloads/castor-advisories/README.md:11`)
  - why: The known state says Resend, Stripe test and Google secrets were pasted into a chat and must be rotated; with no runbook the rotation is a memory exercise across the Stripe dashboard (key + re-register the webhook for the stable host), Resend, Google Cloud (where the callback URI is also still unregistered), and .env + a launchd restart. Meanwhile the docs an operator would follow produce a wrong 
  - proof: A doc test asserting that every variable render.yaml comments as REQUIRED (SESSION_SECRET, CASTOR_PUBLIC_URL) is named in DEPLOY.md fails today.


### Extra items from the skeptics (SK-n, in order)

- SK-1 [reliability] routes/research.py:259-262 ,  resume_interrupted does `except _quota.QuotaExceeded: continue  # ... leave it pending`, but nothing ever reads pending rows again: pending_ids is read once at boot (jobs.py:335 'Nothing polls this; the startup resumer reads it once') and requeue_orphans touches only state='running'. With quota.py:33 MAX_CONCURRENT_RUNS = 1 and quota.py:229-240 refusing any job_id other than the one the slot names, an owner whose deploy interrupted two runs (or one running plus one 
- SK-2 [reliability] plan.py:2353-2359 ,  checkpoint() wraps progress(result) in a bare `except Exception: pass`, so a failing checkpoint write (locked or full DB) is silent while the run continues; the seed resume_interrupted trusts (routes/research.py:252 `seed = row.get("result")`) can then be arbitrarily stale and the resumed run redoes steps it had finished. The terminal publish got a retry and a loud log (jobs.py:462-478); the 27 per-step checkpoints got neither. jobs.py:471's stated safety net ('the orphan sw
- SK-3 [reliability] gates/surface.py:126-131 ,  d14_no_failed_sections detects a failed 4Ps only by the literal string 'generation failed' in each section's narrative. four_ps.py:1181 writes '({name} section timed out)' on a per-section 90s timeout, and a whole-task timeout (plan.py:2612-2618) stores {'error': ...} with no section keys, so `bad == []` and the gate reports 'all sections generated' for a 4Ps that has none. The one invariant named for this failure cannot see either shape of it.
- SK-4 [grounding] skills/price_intel.py:36-41 ,  scrape_market_price is dead in production and the suite cannot see it. `_prices_from_page` does `float(v)` on each item of `extract_prices(...).payload`, but tools/scrape.py:81-92 forwards scrape/structured.py:205-260's list of dicts ({'amount':..,'currency':..}); the TypeError is swallowed by `continue`, prices is always [], and line 93 returns the skeleton. Reproduced on a page with '$49/month' and '$299 per month' -> []. test_price_intel.py:33-36 mocks extract_p
- SK-5 [grounding] intake.py:472-474 ,  the required-fields guard on the intake's 'ready' verdict is `if next_action == 'ready' and missing_required and user_msg_count < 6: next_action = 'ask'`, so after six user turns a model-declared ready passes with product, target_customer, business_model and geography all unset; the session composes final_description, web/survey.js:720-722 posts it if it is 30+ chars, and routes/research.py:533 spends the credit. The reader's G4 covers the curl path; the survey path reaches 
- SK-6 [grounding] report/verifier.py:221-227 ,  a figure whose formula cannot be reconciled (`computed is None or computed == 0`) is ADVISORY, never BLOCK, so the 2.5x formula gate in G3 applies only to figures whose formula safe_eval can parse. A model that writes prose formulas ('national spend times adoption share') escapes the only arithmetic check entirely while the figure still ships and status stays VERIFIED; combined with verify_with_llm=False at standard effort (capabilities/effort.py:40), no layer ever 
- SK-7 [security] `.jobs.sqlite-wal` and `.jobs.sqlite-shm` are untracked and NOT ignored (.gitignore:19 lists only `.jobs.sqlite`; `git check-ignore -v .jobs.sqlite-wal` rc=1). They exist whenever the always-on launchd server holds the database open (this session's opening git status showed `?? .jobs.sqlite-shm` and `?? .jobs.sqlite-wal`), and the WAL carries live database pages (accounts, password hashes, intake text), so a `git add -A` or `git add .` commits a slice of the production database. Fix: add `.jobs.
- SK-8 [security] The live database contains test fixtures, and /feedback/stats serves them publicly: run_ledger holds the key `guest-ip:testclient` (quota.py:141 guest_ledger_key) and feedback holds rows for job_id `job-2`; 14 of the 15 feedback rows reference jobs that no longer exist, and the public /feedback/stats returned 10 of them with comment text today. conftest.py:11 sets JOBS_DB_PATH to a temp file, so either a test path bypasses it or a pre-conftest run wrote to the real .jobs.sqlite (feedback.py:28-4
- SK-9 [security] The reader's SEC-3 cost model does not match the live configuration: llm.py:143-151 excludes `anthropic` (llm.py:117 PAID_BACKENDS) from the fallback chain unless LLM_ALLOW_PAID=1 or LLM_BACKEND names it, and neither is set in .env, so unmetered /intake/start traffic cannot bill the paid key today; the exposure is the shared free-tier RPM budget that every other user's run draws from (llm.py:130-133). The finding stands but the operator should not read it as a dollar risk until LLM_ALLOW_PAID is
- SK-10 [billing] UNLIMITED FREE REPORTS BY CHAINING previous_job_id, independent of refunds (S1). The comment at routes/research.py:513-515 says 'previous_job_id is set only by post_revise, which has already refused a second cycle, so this cannot be used to mint unlimited runs by chaining', but the refusal lives in iteration.reruns_left (iteration.py:470-476, which counts params.previous_job_id and status=='revised') and post_plan calls iteration.spend_rerun (iteration.py:490-495), which reads only reruns_used o
- SK-11 [billing] CREDITS ARE SPENT UNDER CASTOR_PAYWALL_OFF, AND ON STUB CLONES (S3). routes/research.py:533 `if not _revision_of and billing.consume(_owner, "report")` runs before the paywall_off() check at 559, so an account holding credits pays one per run even while the switch says nobody is asked to pay; api.py paywall_off docstring ('the ordinary free daily allowance takes over') is false for credit holders. With CASTOR_STUB_REPORT on, the credit buys a clone: routes/research.py:590 `_stub_run` is inside w
- SK-12 [billing] THE 429 PATH LEAVES A SPENT, UNREFUNDED LEDGER ROW FOR A JOB THAT NO LONGER EXISTS (S4). routes/research.py:537 record_spend(job_id, owner) runs before claim_run_slot at 540; on QuotaExceeded (543-549) the code calls billing.credit_back(owner, 'report', ...) with no job_id and never marks credit_spends.refunded_at, then jobs.discard(job_id). The balance is correct, but the credit_spends row for a deleted job stays refunded_at NULL, so paid_owner(job_id) is truthy and a later refund_for_job(job_i
- SK-13 [product] web/library.html:209-210 ,  the empty-state copy promises 'Run a report and you can be the first. It is worth $10 off your next one.' but routes/jobs.py:1059 pays the reward only when `billing.paid_owner(job_id)` and not refunded, and under CASTOR_PAYWALL_OFF=1 every run is free, so the one sentence a stranger reads on an empty shelf (once UX-1 is fixed) is a promise the server will refuse; the report page's own share card (templates/report.html:2793-2799) already says the opposite.
- SK-14 [product] routes/jobs.py:178 ,  the withheld page offers every reader 'Show it anyway (records the override)' via ?force=1, and get_job_report_html (routes/jobs.py:829-935) has no operator/role check (grep for operator|admin|role|owner in 829-860 returns nothing); the docstring at :848-851 frames force as an operator judgement call ('a demo ... the right call'), yet the founder is handed it in operator voice one click under a page that just told them the report failed its own checks. Only the refund guard
- SK-15 [product] routes/jobs.py:113-114 ,  the remedy form's rerun POSTs `{description:d, operator_weights:{}}` with no `intake` record, while the sibling revise path at routes/jobs.py:546-554 forwards `intake=rec` and the survey's own launch at web/survey.js:743-745 sends `body.intake`; remedy.py's `_intake_resolved(result, field)` reads that record, so a rerun started from the withheld page discards the founder's structured answers and can be withheld again for the same gap. (Mechanism read from the three call
- SK-16 [performance] discover.py:817 `time.sleep(2.5)  # pytrends rate limit courtesy` is unconditional inside _gather_signals, which the discover step runs per candidate on a 4-worker pool (discover.py:913). brand_trend_slope is `@cached("brand_trend")` (sources.py:556), so the sleep fires on cache hits and on the except branch (discover.py:813-815) alike: ~0.6s of pure sleep per candidate on the discover critical path (150-305s), the one step the reader identified as HTTP-bound, and it is not an HTTP wait.
- SK-17 [performance] A run_labeled timeout abandons its worker, and the abandoned worker keeps spending the run's rate budget. capabilities/scheduler.py:64-79 releases the batch with shutdown(wait=False) because 'Python cannot interrupt the worker thread'; that thread continues its call_json re-asks (llm.py:752-770), each taking a 4s reservation in the shared per-model `_gemini_next_free` (llm.py:203-205). So when the 90s sizing cap (plan.py:2038) or the 120s 4Ps cap (plan.py:2612) fires, the financials/viability ca
- SK-18 [performance] The test suite writes into the repo's .transcripts: persistence/transcript.py:41-44 falls back to <repo>/.transcripts unless CASTOR_TRANSCRIPT_DIR is set, and conftest.py never sets it (grep finds no CASTOR_TRANSCRIPT_DIR in conftest.py, paths.py, jobs.py). Result: 3,159 files for 370 jobs, 2,623 of them zero bytes, the newest dated today's test runs (jobs.py:389 opens a TranscriptWriter for every run_async job). Every job runs a mkdir plus a directory of 3k entries, and the working tree accumul
- SK-19 [codehealth] report/render_html.py:296, :299, :314 reach sideways into plan via `__import__("plan").build_integrity_summary(r)`, `.assess_run_health(r)`, `.build_provenance_summary(r)`: a fourth domain->plan edge, and one that both AST layer tests are blind to because they only walk ast.Import/ImportFrom (test_the_layers_only_point_downward.py:74-76,109-110; test_the_frame_does_not_import_the_soul.py:107-114). Any facade-only-import ratchet for plan written the same way passes while this edge stays.
- SK-20 [codehealth] plan.py:2463-2464: because skills/registry.py:175-184 converts a raising skill into Evidence(payload=None, error=...), a failed scale classification stores result["market_scale"] = None and then calls _step_done(result, "market_scale"), so the step ledger (and D01's count) records the step as completed on a run with no decision; the late pass at plan.py:2020-2021 then issues a second LLM call and AttributeErrors on `scale_decision.get("scale")`. Separately, llm.py call_json returns {"_parse_erro
- SK-21 [codehealth] harness/agent.py (349 lines) and harness/__init__.py have no non-test importer at all (only test_harness.py:14,133-157), so the whole harness package is test-only and context/compaction.py is dead through it; the same is true of skills/narration.py and skills/pipeline.py, which production imports (skills/__init__.py:27,35) but never invokes (their skill names 'narration' and 'full_report' appear in no production caller). A dead-module test must walk reachability from api/cli/tools.run_live rathe
- SK-22 [ops] The boot-time coupon push reports success while every push fails, and each restart leaks Stripe objects. sharing.py:266-279 sync_pending() does `_sync_to_stripe(code, row[0]); n += 1` and _sync_to_stripe (:238-248) swallows the exception with a warning, so api.py:391 logs 'pushed 6 coupon(s)' right after six '[sharing] coupon ... is not redeemable yet: 400 Client Error ... /v1/promotion_codes' lines (/tmp/castor_server.log, 12:00:36-12:00:40 today). billing.create_promo_code (billing.py:626-638)
- SK-23 [ops] A container deploy cannot sell bundles: billing.py:44-45 maps bundle5/bundle10 to STRIPE_PRICE_BUNDLE5/STRIPE_PRICE_BUNDLE10 (.env sets both, .env.example:75-76 lists both) but render.yaml's envVars and fly.toml define only STRIPE_PRICE_REPORT/MARKS/QUESTIONS/RERUN (grep STRIPE_PRICE_BUNDLE render.yaml fly.toml -> nothing), so price_for('bundle5') returns None on Render/Fly and the bundle buttons silently have no price.
- SK-24 [ops] The known-state 'redirect_uri_mismatch' is confirmed to be console-side, not config-side: .env GOOGLE_REDIRECT_URI=https://app.castor-advisory.com/auth/google/callback and CASTOR_PUBLIC_URL=https://app.castor-advisory.com match the stable host exactly, so the one fix is registering that URI on the OAuth client (and the rotation runbook OPS-7 asks for should say so, since rotating GOOGLE_CLIENT_SECRET is done in the same console screen).

## Plan

### M0: before the first real-money test

**Gate:** With CASTOR_STUB_REPORT and CASTOR_PAYWALL_OFF both unset on the launchd server: a boot with CASTOR_STUB_REPORT set refuses; one test-mode checkout started from https://app.castor-advisory.com with card 4242 shows report_credits=1 in GET /billing/status within 60s; the run that credit buys is killed with `launchctl kickstart -k gui/$(id -u)/com.castor.server` and after the restart either completes or shows billing.was_refunded true; and ~/CastorBackups holds a file under 24h old that `backup.py --verify` accepts.

- **(S) The boot guard refuses a selling instance that would ship the stub** closes OPS-2
  - fix: Extend api._refuse_to_boot_misconfigured to raise RuntimeError when CASTOR_STUB_REPORT or CASTOR_PAYWALL_PREVIEW is set and either CASTOR_ENV=production or (billing.configured() and not paywall_off()), and under production to require CASTOR_PUBLIC_URL whenever RESEND_API_KEY is set. Keep it inside the one boot step so test_boot_has_one_place_to_read.py still watches a single sequence.
  - proof: test_a_selling_instance_cannot_boot_on_the_stub.py: with STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET and CASTOR_STUB_REPORT set and CASTOR_PAYWALL_OFF unset, entering the lifespan raises RuntimeError; today the lifespan yields.

- **(S) A nightly backup off the repo volume, verified weekly** closes OPS-1
  - fix: Add config/launchd/com.castor.backup.plist (StartCalendarInterval 03:30, same user as com.castor.server so the 0600 .jobs.sqlite is readable) running `.venv/bin/python backup.py --out ~/CastorBackups` plus a prune of copies older than 30 days, and a weekly entry that runs `backup.py --verify` on the newest file into ~/Library/Logs/castor/backup.log. Load it with launchctl now and describe it in the live-path section DEPLOY.md gets in M2.
  - proof: test_the_backup_agent_exists.py parses config/launchd/com.castor.backup.plist and asserts it names backup.py, an --out outside the repo, and a StartCalendarInterval; fails today because the file does not exist. Gate check on the box: newest file in ~/CastorBackups is under 24h old and `backup.py --verify` reports every table matching.

- **(M) Stripe grants what it charges: webhook on the app host, a confirm path that does not depend on it, a boot check** closes M2
  - fix: In the Stripe dashboard register https://app.castor-advisory.com/billing/webhook for checkout.session.completed and checkout.session.async_payment_succeeded, paste its signing secret into STRIPE_WEBHOOK_SECRET, delete the https://www.castor-advisory.com/ endpoint, and in the same .env edit rotate the transcript-pasted STRIPE_SECRET_KEY, RESEND_API_KEY and GOOGLE_CLIENT_SECRET, then `launchctl kickstart -k`. In api.billing_checkout append &session_id={CHECKOUT_SESSION_ID} to success_url, add GET /billing/confirm?session_id= that retrieves /v1/checkout/sessions/{id} with the secret key and calls billing.fulfill idempotently on session_id (web/survey.js resumeAfterPurchase calls it before launching), and have _refuse_to_boot_misconfigured, when billing.configured(), list webhook endpoints and log.error if none equals CASTOR_PUBLIC_URL + '/billing/webhook'.
  - proof: test_a_checkout_is_granted_without_the_webhook.py: with billing's session fetch stubbed to a paid session, GET /billing/confirm?session_id=cs_test_x grants one report credit and a second call grants nothing more; 404 today. Gate: card 4242 from the app host, GET /billing/status reaches report_credits=1 within 60s (stays 0 today).

- **(M) Boot treats every interrupted run as dead, refunds what it cannot resume, and keeps draining pending** closes RR-1, M3, SK-1
  - fix: In api._resume_interrupted_runs call jobs.requeue_orphans(grace_seconds=0); in jobs.requeue_orphans select every running or pending row regardless of age, move rows past RESUMABLE_WINDOW_S or past MAX_RESUMES to state=error with a reason and call billing.refund_for_job(jid, reason) (imported inside the function so layers still point down), delete the retained _cleanup_orphaned_jobs and the 'orphan sweep an hour later' comment at jobs.py:471, and rewrite test_a_live_run_is_left_alone in test_a_deploy_does_not_eat_a_paid_report.py to describe a non-boot sweep or drop it. In routes/research.resume_interrupted replace `except QuotaExceeded: continue` with a drain: jobs.run_async's worker calls resume_interrupted() after releasing _RUN_GATE, so a pending row queued behind a running one starts when the slot frees instead of at the next boot.
  - proof: Three tests in test_a_deploy_does_not_eat_a_paid_report.py: a running plan row with updated_at=now-15 and a checkpointed result is in pending_ids after _resume_interrupted_runs (state stays running today); a running row with _resumes=2 and one with updated_at=now-2d both end state=error with billing.was_refunded true (unrefunded today); two pending rows for one owner with run_plan stubbed to 1s both reach complete without a second boot (the second stays pending today).


### M1: before the collaborator and anyone outside gets the link

**Gate:** A stranger holding the link cannot mint a free run from a previous_job_id, get a refund plus a report, spend a credit on a 30-character brief, read another tenant's feedback, or receive a session cookie over plaintext; the library shelf draws and the withheld page's rerun lands on its job; a report whose sizing, 4Ps or scale decision failed says so and refunds; the Google button either works or is hidden; DELETE /auth/account erases what it promises; the tunnel stays up when the other project's hostname blips; and the repo the collaborator may clone holds no personal file and cannot commit a WAL slice.

- **(S) One included revision, and only of a report that was delivered and paid for** closes SK-10, M1
  - fix: In iteration.spend_rerun refuse when the parent's params carry previous_job_id or its status is 'revised' (counting the way reruns_left already does), when billing.was_refunded(parent) is true, or when the parent halted; in routes/research.post_plan stop reading previous_job_id from the request body and expose a module-level submit_plan(req, owner, previous_job_id=None) that routes/jobs.post_revise calls with the server-built brief. Make GET /jobs/{id} in routes/jobs.py strip `result` (keeping verification.summary) whenever _refuse_forcing_a_refunded_report would refuse the HTML.
  - proof: test_a_refund_does_not_come_with_a_report.py: record_spend then billing.refund_for_job on job A; POST /jobs/A/revise is 402, POST /plan {previous_job_id: A} is 422, GET /jobs/A carries no `result` (all 200 today); and a chain test where revise A->B succeeds once and B->C is 402 with balance 0 (every hop 200 today).

- **(S) Production mode on the live host: https only, Secure cookies, security headers, every operator route behind one gate** closes SEC-1, SEC-5, SK-8
  - fix: On the Cloudflare zone turn on Always Use HTTPS and HSTS; in .env set CASTOR_ENV=production and SESSION_SECRET to the contents of ./.session_secret so nobody is logged out, and restart com.castor.server. In api.py make the cookie Secure flag default on (explicit CASTOR_INSECURE_COOKIES=1 for localhost), add one middleware beside _bind_request that sets Strict-Transport-Security, X-Content-Type-Options: nosniff, X-Frame-Options: DENY and Referrer-Policy on every response, lift the /feedback/stats production 404 into a _production_hidden() dependency applied to /usage, /api/tools, /api/skills, /api/agents, /architecture and /benchmarks, one-off delete the 14 feedback rows whose job no longer exists and the guest-ip:testclient ledger row, and add a conftest assertion that the resolved JOBS_DB_PATH is under the tmp dir.
  - proof: test_the_public_host_speaks_only_https.py: boot with CASTOR_ENV unset, log in, assert the session Set-Cookie carries Secure and the response carries Strict-Transport-Security (neither today); under CASTOR_ENV=production GET /usage, /api/tools and /feedback/stats are 404 (200 today; test_api.py:78 keeps its non-production 200). Live: `curl -sI http://app.castor-advisory.com/login` returns 301 (200 today).

- **(S) No credit leaves the account before the brief is checked, and the slot is claimed before the spend is ledgered** closes G4, SK-5, SK-12
  - fix: In routes/research.post_plan, before billing.consume, validate intake with a Pydantic model whose facts must carry intake.REQUIRED_FIELDS (an all-None record is 422), run a deterministic content check on free-prose briefs (at least three sentences, a product and a customer noun phrase, not one repeated character class), make company_profile.py treat category=='unknown' as an error so the existing error refund fires, and remove the six-turn lift at intake.py:472-474 so 'ready' never passes with required fields missing. Reorder the paid branch to claim_run_slot, then consume, then record_spend, so a QuotaExceeded never leaves a credit_spends row for a discarded job.
  - proof: test_a_junk_brief_costs_nothing.py: POST /plan {description: 'x'*30} for an owner holding one credit is 422 with the balance unchanged (200 and consumed today); a survey session at seven user turns with no required fields does not reach next_action='ready' (it does today); a 429 on submit leaves no credit_spends row with refunded_at NULL (one today).

- **(S) A timed-out sizing, 4Ps, PSM or place is recorded, disclosed, and withholds like any other empty section** closes RR-2, SK-3
  - fix: Give capabilities.scheduler.run_labeled an optional result= argument that calls record_dropped_output(result, label, err) for every joined value carrying error, and pass it at plan.py:2038 and :2612, orchestrator/steps/pricing_sim.py:59 and the place branch at plan.py:2564; add invariants to gates/invariants.py that fail when market_sizing or four_ps is absent without a _dropped_outputs entry, so routes/research._refund_if_nothing_was_delivered withholds and refunds. Make gates/surface.d14_no_failed_sections match '(section timed out)' and a four_ps that is only {'error': ...}.
  - proof: test_a_timed_out_section_says_so.py: patch run_labeled as imported by plan to return {'sizing': {'error': 'timeout after 90s'}}, run run_sizing_stage, assert result['_dropped_outputs']['market_sizing'] names the timeout (KeyError today) and the new invariant blocks; a four_ps of {'error': 'timeout'} makes D14 fail ('all sections generated' today).

- **(S) A missing or errored scale decision is a recorded drop, not a healthy digital venture** closes CH-1, SK-20
  - fix: At plan.py:2463 store the classifier's Evidence error when payload is None (result['market_scale'] = {'scale': None, 'error': ...}, record_dropped_output, no _step_done), guard the late pass at plan.py:2020 against a None decision, and in gate_and_annotate_sizing treat a missing or errored decision on a venture whose profile reads physical as 'skill did not run' (publishable=False, note naming the cause); at plan.py:1341 pass the exception text into the NOT GROUNDED note instead of the fixed 'no street address' sentence. Make D52 return Finding(False) when market_scale carries an error, and have skills/sizing/classify.py stamp defaulted=True on a _parse_error fallback so a defaulted national_digital classification is disclosed rather than presented as a decision.
  - proof: test_a_lost_scale_decision_is_not_a_digital_venture.py: stub llm.call_json to return {'_parse_error': ...} for the classifier on a cafe brief with estimate_market_size stubbed to a digital TAM; assert result['market_sizing']['publishable'] is False, D52 is not None, and the step ledger does not list market_scale as done. Today publishable stays True, D52 abstains, and the step is recorded done.

- **(S) The library shelf draws, and its empty-state promise matches what the server pays** closes UX-1, SK-13
  - fix: In web/library.html move `<script src="/castor-api.js">` above the inline IIFE (or wrap the IIFE in DOMContentLoaded), and make the '$10 off your next one' sentence conditional on /billing/status needs_purchase so a free-tier visitor is not promised a reward routes/jobs.py:1059 refuses. Replace the string assertions in test_a_failure_never_looks_like_an_answer.py with a source-order test now and a node harness test once the M2 harness lands.
  - proof: test_a_failure_never_looks_like_an_answer.py gains assert src.index('<script src="/castor-api.js"') < src.index('window.castor.api(') (fails today); a node/jsdom run of the page against one /library.json entry draws a .vol element (TypeError today).

- **(S) Answer & rerun lands on the new run and carries the founder's intake** closes UX-2, SK-15
  - fix: In routes/jobs._remedy_form_html parse the body once (`var out=await r.json(); if(!r.ok) throw new Error(out.detail||r.statusText); location.href='/progress.html?job='+encodeURIComponent(out.job_id)`), include the job's intake record in the POST body the way post_revise threads intake=rec, and link the new job id from the withheld page. Add `<meta name="viewport">` to _withheld_page and the halt page.
  - proof: test_the_withheld_page_reruns_into_the_new_job.py renders _withheld_page with one remedy and asserts 'r.job_id' is absent, job_id is read from the parsed body, the POST body carries intake, and a viewport meta is present; no test covers _remedy_form_html today and 'r.job_id' is in the output.

- **(M) DELETE /auth/account erases everything the docstring promises** closes SEC-2
  - fix: In auth.delete_account collect the account's job ids before deleting jobs, then delete iteration, feedback, shared_reports and credit_spends rows for those ids, coupons and auth_tokens rows for the account, and unlink persistence.transcript.path_for(job_id) for each, returning the per-table counts. Bring auth.py:436 and api.py:964-980 docstrings in line with what the code does.
  - proof: test_a_deleted_account_leaves_nothing_behind.py: create an account and job, add an annotation via iteration.add_annotation, a feedback row, a shared_reports row and a transcript file, delete the account, assert all four are gone and /library.json no longer lists the title; today every one survives.

- **(M) The LLM chain respects the batch deadline, a join timeout counts as exhaustion, and a second free provider shares the budget** closes RR-3, SK-17
  - fix: Pass a deadline from capabilities.scheduler._call_with_timeout into llm.call_json (a threading.Event checked between chain attempts at llm.py:495-497 and before each _gemini_rate_gate reservation) so an abandoned worker stops re-asking and stops reserving 4s slots, and have _call_with_timeout call llm.note_exhaustion on a join timeout so result['_llm_exhaustion'] tells the reader the pipeline could not look. Set GROQ_API_KEY in .env (empty today) so fallback_chain() is not one model's 15 RPM, and size the run_labeled timeouts at plan.py:2038/:2612 and pricing_sim.py:59 from expected calls x gate interval x chain worst case.
  - proof: test_a_step_that_ran_out_of_time_says_so.py: stub llm._try_one_backend to sleep 35s per attempt, run run_sizing_stage under the real 90s join, assert result['_llm_exhaustion'] or _dropped_outputs['market_sizing'] mentions the timeout and that _gemini_next_free is not advanced after the join returned; today the result carries neither and reservations continue.

- **(S) One paywall rule, and every meter and sentence derived from it** closes M4, SK-11, UX-7
  - fix: Decide the rule (recommended: a selling instance honours the daily free allowance, then 402) and implement it once in routes/research.post_plan by checking paywall_off() before billing.consume so a credit is never spent while the switch says nobody pays, and by falling through to claim_run_slot(count_daily=True) while the allowance has room; refund the two unrefunded clone spends on account f6e138be. Derive web/login.html's free-tier sentence, web/home.html's Buy control and free_runs_left in api.billing_status from _needs_purchase, and source the step count in landing.html and progress.html from PHASES.
  - proof: test_the_meter_and_the_gate_agree.py: with Stripe keys set and PAYWALL_OFF unset, GET /billing/status never returns needs_purchase=true with free_runs_left>0 (3 today); under CASTOR_PAYWALL_OFF=1 a credit holder's POST /plan leaves the balance unchanged (consumed today) and /home draws no Buy control; /login rendered on a selling instance does not contain 'Three free reports a day'.

- **(S) Google sign-in either works or is not drawn** closes UX-4, SK-24
  - fix: In the Google Cloud console register https://app.castor-advisory.com/auth/google/callback (and http://127.0.0.1:8765/auth/google/callback for dev) on the OAuth client; the client secret rotation from M0 is on the same screen. Add GOOGLE_SIGNIN_ENABLED to .env that api.google_configured() requires alongside the two keys, so /auth/me reports google:false and web/login.html keeps the button hidden while a registration is missing.
  - proof: test_a_half_configured_google_button_is_not_drawn.py: with client id and secret set and GOOGLE_SIGNIN_ENABLED unset, GET /auth/me returns google=false and GET /auth/google is 404 (google=true and a 302 today). Live: following the 302 ends on /home or /login, not a Google 400.

- **(S) A failed run explains itself in the list and in a sentence, not a Python identifier** closes UX-5
  - fix: Add error to jobs.list_recent's SELECT and dict so GET /jobs carries it for state='error' rows, and in web/progress.html fail() and the halt page in routes/jobs.py map anything matching ^[A-Z][A-Za-z]+Error: to a reader sentence while keeping the raw text on the job for operators.
  - proof: test_a_failed_run_is_explained_to_the_founder.py: seed state='error', error='NameError: name checkpoint is not defined'; assert GET /jobs returns a non-empty error on the row and GET report.html does not contain 'NameError'; fails on both today.

- **(S) The contact form only says received when something was** closes UX-3
  - fix: Add POST /contact in routes/pages.py that relays via mailer.send to MAIL_FROM's inbox behind a per-IP limiter, and in web/landing.html show 'Message received' only on a 2xx, otherwise offering the mailto: link with copy that says it opens the visitor's mail app.
  - proof: test_a_message_is_received_only_when_sent.py: POST /contact with mailer.send stubbed to raise returns a 5xx (404 today), and the landing source emits the success copy only inside a fetch-resolved branch (unconditionally today).

- **(S) Castor owns its tunnel and its caffeinate** closes OPS-3
  - fix: Create a Castor-owned named tunnel carrying only the castor-advisory.com hostnames so tunnel-keeper.sh's `pkill -f 'cloudflared.*tunnel run shuttle'` no longer matches com.castor.tunnel, and add config/launchd/com.castor.caffeinate.plist running `caffeinate -s -i -m` with KeepAlive. Document lid-close and logout as unsupported in DEPLOY.md until a container path is live.
  - proof: test_the_castor_agents_stand_alone.py parses config/launchd/*.plist and asserts the tunnel invocation does not contain 'run shuttle' and a caffeinate agent exists (fails today). On the box: `pgrep -fc 'tunnel run'` stays constant while shuttle.principleofprocrastination.com is made to fail twice (1 -> 0 -> 2 today).

- **(S) The repo carries no personal file and cannot commit a database slice** closes SEC-6, SK-7
  - fix: `git rm --cached feedback-for-jane.pdf feedback-for-jane.txt` and add those paths, `.pytest_cache/` and `.jobs.sqlite-*` to .gitignore. If the repo is ever made public, rewrite history for the two feedback paths.
  - proof: test_the_repo_carries_no_personal_file.py asserts `git ls-files` matches no feedback-for-jane path and `git check-ignore .jobs.sqlite-wal` exits 0; both fail today.


### M2: before public launch

**Gate:** A buyer's headline changes when its anchoring source's tier changes; a figure carrying calc more than 25% off its own arithmetic is blocked; the price scrape produces a price; one benchmark report has been scored against a realized outcome with the stub off; a revision preserves every section the founder did not mark and reaches settle when resumed; the share reward is redeemable and the boot push counts only successes; every LLM endpoint is metered; ?force=1 is an operator control; /readyz is pinged from outside and logs survive a reboot; the checkout and gate JS is executed by a test; and DEPLOY.md names every REQUIRED variable and how to rotate each secret.

- **(M) Source tiers reach the headline** closes G1
  - fix: Pass rule=PRECISION_WEIGHTED at market_sizing.py:158 and skills/sizing/triangulation.py:79 (or, if the median is to stay, call source_tiers.can_anchor_sizing at both sites to exclude CONTENT_MILL/PADDING/COMMUNITY methods and disclose the exclusion in derivation prose the way unit conflicts are at forecast.py:393-398), and rewrite TestDefaultIsUnchanged in test_a_filing_outweighs_a_storefront.py, which pins the old default on purpose. Show the tier of each anchoring source in the report's trust panel so the reader can see what the rule did.
  - proof: test_a_storefront_and_a_filing_are_not_one_vote_each.py: market_sizing.apply_tam_triangulation on two origin-distinct methods, one sourced to sec.gov and one to deepmarketinsights.com, gives a different headline than both sourced to sec.gov; today both return the same mid.

- **(M) The verifier blocks wrong machine arithmetic, checks one number from outside the model, and reads the baselines it writes** closes G3, SK-6
  - fix: In report/verifier.py block figures carrying `calc` outside the 0.8-1.25 band (keeping 2.5x only for prose formula), and stamp a figure whose formula cannot be reconciled as unchecked=true with a reason the trust panel shows, recomputing it when verify_with_llm is on (turn that lever on for standard effort in capabilities/effort.py:40). Add one non-trade-area external plausibility gate in the D57 shape (tam.mid against competitor roster size x median scraped price x 12; SOM Y1 against the median competitor's estimated revenue, both from the discover roster and price_intel already fetched), and give gates/runner.py a --baseline path that diffs a scorecard against docs/baselines, or delete that directory and the promise at gates/invariants.py:9.
  - proof: test_the_arithmetic_gate_has_no_rounding_excuse.py: a figure with value_usd=1,000,000 and calc='1200000 * 2' yields a BLOCK (no finding today); a figure with formula 'national spend times adoption share' carries unchecked=true (silently ADVISORY today); gates.run_gate --baseline docs/baselines/M3.json reports a regression on a doctored scorecard (no such argument today).

- **(S) The price scrape works and its test cannot lie about the shape** closes SK-4
  - fix: In skills/price_intel._prices_from_page read each item's 'amount' (and currency) from the dicts scrape/structured.py returns instead of float(v), and change test_price_intel.py's mock of extract_prices to return the list of dicts the real tool produces. Re-run the corpus through tools.run_live and confirm arpu_origin='scrape' occurs at least once so the price_intel section the report promises can exist.
  - proof: test_a_scraped_price_reaches_the_arpu.py: a page with '$49/month' and '$299 per month' through the real extract_prices yields two prices from scrape_market_price; [] today and the section is absent from every corpus report.

- **(S) The share reward is redeemable and the boot push tells the truth** closes M5, SK-22
  - fix: In billing.create_promo_code log Stripe's error.message on a 4xx (as create_checkout does), send the promotion_code request in the current API shape (promotion[type]=coupon, promotion[coupon]=id), look up an existing code by `code` before creating, and delete the coupon object when the promotion_code POST fails; make sharing.sync_pending count only rows whose stripe_promo_id became non-NULL. Then rerun the push for the six pending codes and delete the 51 orphan 'Shared a report ($10)' coupons in the Stripe test account.
  - proof: test_a_coupon_push_that_failed_is_not_counted.py: with Stripe stubbed to 400 every promotion_code POST, sync_pending() returns 0 (6 today) and the log carries Stripe's error.message (discarded today); with the stub accepting the current request shape, the row's stripe_promo_id is set.

- **(S) Back from the reading stage resumes the interview; refusals are announced** closes UX-6
  - fix: In web/survey.js keep `session` when Back lands on stage 0 and, on Continue with unchanged prose, loadCard() the existing session instead of POST /intake/start, asking 'Start over? Your answers so far will be discarded' before creating a new one when the prose changed. Add role="alert" to #err in web/survey.html.
  - proof: A node harness test (from the CH-5 item) reaching stage 2, pressing Back, then Continue with unchanged prose asserts /intake/start was called once (twice today); a source test asserts #err carries role="alert" (absent today).

- **(S) A hyperlocal run does not buy a national sizing it will throw away** closes LC-2
  - fix: In plan.run_sizing_stage call size_by_scale first and run _sizing_task plus ground/triangulate/gate only when it returns None, keeping the digital chain as the documented no-address fallback. Keep ground_sizing_bottom_up's price_intel publish or its recorded drop on the hyperlocal path so D54 still sees price_intel as produced or declared absent.
  - proof: test_a_cafe_is_not_sized_as_a_national_app_first.py: scale=hyperlocal with a geocodable site and tools stubbed asserts market_sizing.estimate_market_size is never called and result carries price_intel or a _dropped_outputs entry for it; today six sizing calls run and are replaced.

- **(S) Silent excepts on the delivery path fail toward the safer outcome, and money failures leave a row an operator sees** closes CH-3, SK-2
  - fix: In routes/research._notify_owner set withheld=True when blocking_findings raises, and in _refund_if_nothing_was_delivered treat that raise as a blocking finding so a verifier crash refunds rather than ships; turn the bare passes at plan.py:1903/2675/2683, routes/jobs.py:1152 and checkpoint() at plan.py:2353-2359 into log.warning(exc_info=True), with checkpoint also counting failures on the result so resume_interrupted can tell a stale seed. Persist a 'needs operator' row when refund_for_job, billing.fulfill's grant or sharing._sync_to_stripe fails (surfaced by /readyz below), and add a ratchet test that pins the count of `except Exception:\n    pass` in non-test files.
  - proof: test_a_verifier_crash_withholds_rather_than_ships.py: patch report.verifier.blocking_findings to raise, run _notify_owner on a result with blocking findings, assert mailer.send_report_withheld was called (send_report_ready today) and the refund fired (skipped today); the ratchet fails on any new silent pass.

- **(M) A revision starts from the report it revises; a question is answered from the sections it touches** closes G6
  - fix: In routes/jobs.post_revise pass the prior result as resume_from through submit_plan (from the M1 revision item) into run_plan, invalidating only the steps whose declared inputs the input_edits or annotations touch, so unmarked sections carry over verbatim and result['_previous'] is visible to the revision narrative. In iteration.draft_answers route by based_on: hand the touched sections whole and the rest as the digest, and raise the cap for effort=deep.
  - proof: test_a_revision_keeps_what_the_founder_did_not_mark.py: post_revise on a finished job with run_plan stubbed asserts the stub received resume_from carrying the prior market_sizing (today only the amended description, geo, weights, effort and intake); draft_answers on a question naming competitor #31 of a 40-row roster receives that row whole (truncated today).

- **(M) A resumed run finishes the way a fresh one does, and revise goes through a function, not a handler** closes RR-4, CH-7
  - fix: Lift routes/research.post_plan's _finish closure into a module-level finish_run(job_id, owner, params, result, paid) that both the fresh worker and resume_interrupted's worker call (passing refine=params.get('refine') on resume and consulting _stub_run when CASTOR_STUB_REPORT is set), with routes/jobs.post_revise calling submit_plan rather than post_plan through the api facade. Delete the 11 unreachable `if not _owned_job` branches in routes/jobs.py and replace the 19 call-time `from api import` lines under routes/ with three shims in a new routes/identity.py.
  - proof: test_a_resumed_run_is_still_a_paid_run.py gains: requeue an interrupted revision row with carried questions, run resume_interrupted with run_plan stubbed, assert iteration.get_state(job_id)['status']=='final' (never final today); a coverage run over the routes tests shows the 11 raise lines uncovered before deletion.

- **(L) One report scored against a realized outcome** closes G5
  - fix: Add an optional `realized` block ({metric, value, as_of, source_url, tier}) to the benchmark case schema, populate it for the six tier3_*_real cases from public ARR reporting and for two hyperlocal corpus ventures if the owner knows the operator, and add score_outcome() to benchmarks/score.py comparing som.mid and financials Y1 to the realized value by log10 distance, weighted like score_tam. Narrow score_tam's pass band to <=3x or score by log distance from ref_mid, rename tam_us_corporate_wellness_usd_* to tam_usd_* across the 17 cases, and run benchmarks/run_all.py once with the stub off, recording the result under docs/.
  - proof: test_a_benchmark_carries_an_outcome.py: score_tam with tam.mid=$10B against refs 0.5B/3B/10B is < 100 (100 today), and at least one case file carries a realized block (none today).

- **(S) Every LLM call outside POST /plan is metered** closes M6, SEC-3, SK-9
  - fix: Wrap routes/jobs.post_regenerate_section and post_iterate in routes/research._meter with bucket=quota.AUX_BUCKET (released in a finally around the LLM call), and count POST /intake/start and /intake/{id}/locate in a per-IP bucket keyed like check_login_allowed. Note in quota.py's docstring that the exposure today is the shared free-tier RPM budget, since LLM_ALLOW_PAID keeps the paid key out of the chain.
  - proof: test_a_loop_on_the_cheap_endpoints_hits_a_ceiling.py: 50 POST /intake/start from one client with the LLM stubbed sees a 429 before the 50th (all 200 today); _aux_daily_limit()+1 POSTs to /jobs/{id}/regenerate see a 429 on the last (200 today).

- **(S) The step label follows every fan-out, and sizing and 4Ps have one** closes RR-5
  - fix: In capabilities.scheduler wrap each submitted fn in contextvars.copy_context().run (the pattern four_ps.py:1170-1175 already uses), do the same at the evidence.py:193-201 submits and the other pool sites, and wrap run_sizing_stage and the 4Ps join in step_scope('sizing') and step_scope('four_ps'), since plan.py sets no label for them.
  - proof: test_a_fan_out_keeps_its_step_label.py: enter step_scope('pricing'), submit ledger.current_step through run_labeled, assert the worker returns 'pricing' (None today); a stubbed sizing run files its llm events under 'sizing' (None today).

- **(S) Show it anyway is an operator control** closes SK-14
  - fix: In routes/jobs.get_job_report_html honour ?force=1 only for an operator account (an OPERATOR_ACCOUNT_IDS env list, or CASTOR_ENV != production) and remove the 'Show it anyway' link from _withheld_page for founders, keeping _refuse_forcing_a_refunded_report.
  - proof: test_the_override_is_not_offered_to_the_founder.py: a founder's GET report.html?force=1 on a withheld report is 403 and the withheld page contains no force link; today 200 and the link is drawn.

- **(S) The site can tell someone it is down** closes OPS-6
  - fix: Add GET /readyz in routes/pages.py that opens the jobs DB read-only and reports the worker/resume state and any 'needs operator' rows, and point a Cloudflare health check or a healthchecks.io ping from a launchd StartInterval agent at it; change com.castor.server.plist to log at info into ~/Library/Logs/castor/ with newsyslog rotation instead of /tmp.
  - proof: test_readiness_is_not_a_constant.py: GET /readyz is 200 with the DB present and 503 with JOBS_DB_PATH pointed at an unopenable path (404 for both today); the plist no longer names /tmp.

- **(M) The web JS is executed by a test** closes CH-5
  - fix: Add a dependency-free node:test harness (load web/survey.js, castor-api.js and account.js in a vm context with stubbed fetch and document) run from one pytest test via subprocess the way test_a_survey_survives_a_second_worker.py already does, and port the gate/buy/poll/clock/transport assertions from the six web-grep files into about 15 behaviour tests. Delete test_the_report_is_paid_for_before_it_runs.py:144, the six run_path_source tests (their helper moves out of plan.py in M3), and the CSS pins in test_the_gate_answers_every_click.py.
  - proof: The harness test that calls survey.js gate() with fetch answering 402 and asserts no POST /plan is issued; today no test fails when gate()'s runtime behaviour changes while its text stays.

- **(M) The docs describe the product that is live** closes OPS-7
  - fix: Rewrite DEPLOY.md in three parts: the live path (launchd agents including backup and caffeinate, the named tunnel, every .env switch and what it means), the container path with every render.yaml REQUIRED variable and the boot guard, and a rotation runbook per secret (Stripe key plus webhook re-registration, Resend, Google client secret on the same console screen as the callback URI, .session_secret logs everyone out) ending in `launchctl kickstart -k`. Trim README.md to the current product (port 8765, current test count, current name) and archive SHIP.md.
  - proof: test_the_deploy_doc_names_every_required_variable.py: every variable render.yaml comments as REQUIRED is named in DEPLOY.md (SESSION_SECRET and CASTOR_PUBLIC_URL are not today) and README.md does not say port 8000 (it does today).


### M3: later, or only if X

**Gate:** Each item names its trigger; none is required for launch. Run the container item before anyone connects the Render Blueprint or follows .env.example's CASTOR_TRUST_PROXY line; run concurrency only when /readyz shows a run queued behind another more than once a week; run the plan.py split before the fan-out; run COGS pricing when a paid tier is switched on.

- **(M) The container path builds and is safe, only if Render, Fly or Docker is deployed** closes OPS-4, OPS-5, SEC-4, SK-23
  - fix: Merge origin/master's four commits into the branch by hand on git 2.15 (pricing_model.py and orchestrator/steps/pricing_model_step.py placed at root, plan.py/discover.py/report.html conflicts resolved, or a commit recording them superseded), fast-forward master and push; mirror .gitignore's local-state block into .dockerignore (.session_secret, data/, claude-code-main/, .transcripts/, backups/, *.sqlite-wal, *.sqlite-shm, feedback-for-jane.*); add STRIPE_PRICE_BUNDLE5/10 to render.yaml and fly.toml. Delete the app-level X-Forwarded-For parser in api._client_ip, set --forwarded-allow-ips to the proxy's addresses (CF-Connecting-IP behind Cloudflare) instead of '*', reword .env.example:48-50 and Dockerfile:52-56, and test-build once with `playwright install --with-deps chromium` in place of the hand apt list.
  - proof: `git show master:Dockerfile` succeeds (errors today); test_the_image_carries_no_local_state.py asserts .session_secret and data/ are in .dockerignore (absent today); with CASTOR_TRUST_PROXY=1 eleven wrong logins with a rotating X-Forwarded-For are refused on the eleventh (never today); price_for('bundle5') is non-None under render.yaml's env list (None today).

- **(S) The 4Ps join harvests a late answer** closes LC-1
  - fix: Extract the per-section timeout at four_ps.py:1179 into a constant, replace the sequential fut.result(timeout=90) with concurrent.futures.wait on one shared deadline aligned with the 120s run_labeled cap, harvest any future that is done() before writing a placeholder, and record which attempt timed out on the section.
  - proof: test_a_late_section_is_kept.py: a fake _run_section returning 0.3s after a 0.2s per-section timeout inside a 1s pool budget leaves the section text in the assembled plan, not '(product section timed out)'; fails today once the constant exists to patch.

- **(S) COGS prices the models actually used, only when a paid tier is switched on** closes LC-6
  - fix: Add PRICING rows in llm.py for every id in BACKEND_DEFAULTS and _call_gemini's fallback list (free tier as an explicit 0 with a note), log a warning when ledger.cogs() meets an unpriced model, and route call_text through the same record_llm/usage path as _chain_text. Trigger: LLM_ALLOW_PAID=1 or a paid Gemini tier.
  - proof: test_every_model_in_the_chain_is_priced.py: every model id in BACKEND_DEFAULTS and the Gemini fallback list has a PRICING entry (four missing today).

- **(S) One claim_guest, in the order that cannot strand a draft** closes CH-2
  - fix: Add auth_claim.claim_guest(guest, account_id) that does record_move, jobs, sharing, intake, then billing last with per-step logging, called from both api._set_session and _claim_prepaid; have sharing.reassign_owner log like its siblings. Trigger: any 'could not claim guest work' warning in the log.
  - proof: test_a_failed_claim_is_finished_by_the_next_one.py: sign up with a guest holding one entitlement, one job and one draft while intake.reassign_owner is patched to raise once, then hit /auth/verify; intake.drafts(account) is non-empty (empty today because billing rows moved first).

- **(S) Dead modules are found by reachability, not by importer count** closes SK-21
  - fix: Add a test that walks imports from api, cli and tools.run_live and lists modules never reached (harness/, context/compaction.py, report_audit.py, harness_gates.py, benchmarks/judge_alignment.py, agents/contracts.py, skills/disclosure.py, plus skills registered by import but invoked by no production caller such as narration and full_report), then delete each or document a KEEP reason beside it.
  - proof: test_every_module_is_reachable_from_an_entry_point.py fails today naming those modules.

- **(S) The suite pays for no sleep it mocked away** closes LC-5, SK-16, SK-18
  - fix: Read DEFAULT_MAX_RETRIES at call time in net.request (max_retries: int | None = None), and route scrape/http._throttle and the failure-path sleeps in sources.py, tools/geo.py, reddit_signal.py and discover.py:817 through one pace() helper that the offline fixtures make a no-op and that skips the pytrends courtesy sleep on a cache hit or an except branch. Set CASTOR_TRANSCRIPT_DIR to a tmp dir in conftest.py, delete the test-job files under .transcripts, and optionally add pytest-xdist.
  - proof: test_the_offline_suite_does_not_sleep.py: under the offline fixtures net.get('https://www.trustpilot.com/x') returns in under 1s (15.6s today) and 13 mocked fetches of one host finish in under 1s (6.3s today); after a test run no new file appears under the repo's .transcripts (thousands today).

- **(M) plan.py is four modules, and nothing below it reaches up** closes CH-4, SK-19
  - fix: Move plan.py:442-821 (pricing reconciliation) to pricing_reconcile.py, _venture_location and _fermi_service_model with the geo helpers to orchestrator/steps/venture_geo.py, build_provenance_summary/build_integrity_summary/assess_run_health to report/summaries.py, and run_path_source into the wiring test; then point gates/pricing.py:271, orchestrator/steps/evidence.py:63, preview.py:242 and report/render_html.py:296-314 at the new homes. Add a facade-only rule to test_the_layers_only_point_downward.py that walks ast.Call for __import__ as well as Import/ImportFrom, so the render_html edge is visible; do this before the fan-out below and after the M1/M2 plan.py edits have landed.
  - proof: The facade-only test fails today at the four sites (three ImportFrom plus the __import__ at render_html.py:296) and passes after the extraction; the existing 11 layer tests stay green.

- **(M) The post-discover fan-out, only after the plan.py split and the step-label fix** closes LC-4
  - fix: Restructure run_personas_step, attach_consumer_research, run_differentiators_step and run_max_diff_step to return payloads instead of mutating result and checkpointing, then dispatch them through one run_labeled fan-out after run_evidence_step with the parent applying and checkpointing once; start size_by_scale's geo half right after discover and merge it in run_sizing_stage. Keep PSM -> economics -> sizing -> 4Ps -> financials -> viability serial.
  - proof: test_the_independent_steps_overlap.py: stub the four step functions to record start/finish order and assert all four start before any finishes (serial today), and a checkpoint taken during the fan-out never raises 'dictionary changed size during iteration'.

- **(L) Concurrent generation, only if two buyers overlap** closes LC-3
  - fix: Make the ledger per run (a RunLedger in a ContextVar set in _run_one, carried into pools by the copy_context from the RR-5 item, with @tool and llm recording to the current ledger), then widen _RUN_GATE to a small N and expose queue position on GET /jobs/{id}; or run generation in a small process pool so each run owns its module globals. Trigger: /readyz shows a pending row waiting behind a running one more than once a week.
  - proof: test_two_buyers_do_not_wait_for_each_other.py: POST /plan for two owners with a 2s stubbed run asserts both rows read running within 1s (the second stays pending today).


## Sequencing

M0 execution order differs from its severity order: land OPS-1 (backup agent) first because it protects the only copy of the DB before any other change touches it; OPS-2 (boot guard), the boot-sweep rewrite (RR-1/M3/SK-1) and the /billing/confirm code (M2) touch disjoint files (config/launchd, api.py:349, jobs.py plus api.py:368, api.py:1134-1214 plus survey.js) and can be built in parallel; the operator's dashboard work (register the app-host webhook, delete the www endpoint, rotate the three pasted secrets, paste the new signing secret, set GROQ_API_KEY for RR-3) is one .env edit and one `launchctl kickstart -k` that must precede the checkout test, and the sweep must precede the kill-mid-run test. Three fixes obsolete other items: (1) run_labeled(result=) plus the required-section invariants in the RR-2 item make SK-3's D14 patch a one-liner, absorb half of CH-1 (drop recording), remove LC-1's 'record which attempt timed out', and turn RR-3's proof into the same test (a task exceeding the join asserts exactly what RR-2 records); (2) the spend_rerun rewrite plus submit_plan in the SK-10/M1 item closes CH-7's post_revise-calls-a-handler half and is the seam G6's resume_from and RR-4's finish_run plug into, so those two M2 items depend on it; (3) the boot-sweep rewrite deletes _cleanup_orphaned_jobs, the pinned test_a_live_run_is_left_alone and the phantom 'orphan sweep an hour later' comment, and is what makes RR-4's resumed-run path exercised at all. Within M1 the billing items (SK-10/M1, G4/SK-5/SK-12, M4/SK-11/UX-7) all edit routes/research.py:510-570 and must land serially on one branch in that order; everything else in M1 runs in parallel tracks: ops (SEC-1 env and Cloudflare toggles, UX-4 console, OPS-3 tunnel, SEC-6 hygiene), report honesty (RR-2 then CH-1 then RR-3, all in plan.py and the scheduler), pages (UX-1, UX-2, UX-5, UX-3), and SEC-2. In M2, build the CH-5 node harness first because it is the proof vehicle for UX-6, the paywall gate and the library page; do OPS-6 (/readyz) before or with CH-3 because the 'needs operator' rows surface there; G5 (L) can start the day the stub is off in M0 since it is data collection plus a scorer; OPS-7 (docs) goes last in M2 but the rotation runbook should be written as the M0 rotation is performed, not reconstructed later. M3 ordering: CH-4 (plan.py split) before LC-4 (fan-out), and RR-5's copy_context pattern (M2) before both LC-4 and LC-3; the master merge inside the container item should happen before anyone else clones master expecting the product, since master has never received the branch and the live server runs from the branch's working copy. Skeptic items folded rather than given their own row: SK-9 into the M6/SEC-3 item as a docstring correction, SK-17 into RR-3, SK-22 into M5, SK-24 into UX-4, SK-2 into CH-3, SK-16 and SK-18 into LC-5, SK-19 into CH-4, SK-20 into CH-1, SK-23 into the container item.

## Refuted

- G2 (one scraped price becomes a 'census' origin): the pipeline cannot produce that block because skills/price_intel drops every scraped price (SK-4), so the 'census' stamp only ever marks a founder-stated price times a fetched Census count, which is what plan.py:994-999 intends.
- CH-6 (nine modules with no non-test importer): four of the nine are imported in production (skills/narration and skills/pipeline via skills/__init__, harness/refine via harness/__init__, context/compaction via harness/agent), so the stated importer-count proof would not fail; the reachability version survives as SK-21 in M3.

## What this audit did not examine

- MODALITY, browser: no page was ever rendered. The live /library HTML (fetched read-only) has the inline IIFE at line 181 and <script src=/castor-api.js> at line 234, so UX-1 is live, but nobody saw the blank shelf, the survey, progress, the withheld page, or the report on a phone viewport; no accessibility, contrast, or reduced-motion check.
- MODALITY, real money: no Stripe test-mode checkout, no webhook signature verification, no /billing/status transition, no refund path observed; credit_spends has 2 rows, both refunded_at NULL, and nobody checked whether the 9 'orphaned by server restart' + 1 'killed by restart' error rows in the DB were paid.
- MODALITY, email: mailer.py (174 lines, Resend at api.resend.com, plus MAIL_PROVIDER alternates) is mentioned by 4 test files; no report-ready, withheld, verify-email, password-reset or coupon mail was ever sent or received. web/forgot.html and web/reset.html and the /auth/forgot, /auth/reset, /verify flows are not in the plan at all.
- MODALITY, PDF: report/pdf.py (344 lines) picks weasyprint or chromium at runtime (available_engine); GET /jobs/{id}/report.pdf at routes/jobs.py:939 was never called on this Mac, so it is unknown whether the live host can produce any PDF. Same for /jobs/{id}/onepager.html (templates/onepager.html), /compare (templates/compare.html), /sample, /jobs/{id}/trace, /jobs/{id}/events, /share and /library/{id}/report.html.
- MODALITY, load and concurrency: no two overlapping submissions, no test of _RUN_GATE queueing on the live host, no measurement of Gemini free-tier RPM under one real run (diag01.json carries a _cogs block nobody read).
- MODALITY, output quality: still unmeasured, but the raw material exists and was not opened: out/live/diag01.json (written today 12:18 by tools.run_live, a real run_plan that bypasses the API and the stub) is a hyperlocal venture with tam.mid $150M, 12 advisory findings, 15 blind gate ids (D09, D11, D18, D19, D21, D22, D24, D25, D29, D30, D35, D36, D39, D44, D45) and three recorded drops; nobody read the prose or the sizing derivation.
- MODALITY, production logs: /tmp/castor_server.log is 11 lines, all from today's 12:00 boot; launchd gives no history, so the 'section sometimes misses its run' item cannot be diagnosed from logs and the audit never said so. ~/Library/Logs/castor does not exist.
- MODALITY, edge and DNS: Cloudflare zone settings were never viewed (verified: http://app.castor-advisory.com/login answers 200 with no redirect); the deployed www site on Cloudflare Pages was not examined (a read-only fetch of https://www.castor-advisory.com/ shows no href to app.castor-advisory.com, so how a visitor reaches the app is unknown); the tunnel's behaviour on reboot, lid-close, or logout was not exercised.
- SUBSYSTEM, the report itself: templates/report.html (3658 lines, 428 output tags, 768 control tags) is never named except as a merge conflict; report/render_md.py, citation.py, claim_support.py, section_provenance.py, trace.py and charts.py are not named.
- SUBSYSTEM, the numbers a buyer reads: financials.py (696), economics.py (387), pricing.py (551), business_model.py (554), macro_anchors.py (520), segment_scoring.py (281), competitor_pricing.py (236) and orchestrator/steps/financials_step.py, economics_step.py, viability.py are not named; G5 proposes scoring 'financials Y1' against realized outcomes without anyone having read how Y1 is computed.
- SUBSYSTEM, 14 of 17 orchestrator step modules unnamed: personas, clustering, competitor_refinement, segments, profile, max_diff, differentiators, crew, economics_step, customer_universe, viability, financials_step, competitors, firmographics (plan names only evidence.py, pricing_sim.py and the master-branch pricing_model_step.py). Root modules customer_universe.py (1065), intake_tree.py (656), taste.py (513), differentiators.py (456), slots.py (431), brief.py, history.py, place.py, remedy.py, feedback.py, url_guard.py, cache.py, match.py, provenance.py, rendering.py also unnamed.
- SUBSYSTEM, external data tools: tools/ has 19 files (ads, customer_voice, domain, econ, firmographic, geo, gmaps_reviews, overture, social, trend, scrape, sources/) plus scrape/crawl.py, search.py, wayback.py; today's boot log shows meta_ad_library and rank_meta_advertisers auto-detected as tier=metered with 'verify cost_usd is set correctly' and gmaps_ratings as concurrency=mutating; none of this was examined. The DB's error rows include 'hung on waybackpy' and 'hung on Crunchbase wayback'.
- SUBSYSTEM, sizing inputs: data/public holds Census zips (qcew_2023_annual, zbp21detail, nonemp22msa, nonemp22co, pums_2022_hus) with a download.log; vintage, licence and how they feed size_hyperlocal / trade_area_catchment were not examined.
- SUBSYSTEM, agents and skills: agents/crew.py, planner.py, research_agents.py, synthesis.py; skills/discovery.py, discovery_multi.py, perspective.py, refine_report.py, triangulate.py, registry.py; capabilities/gateway.py (245) and safe_call.py; core/evidence.py and core/registry.py; model/tiering.py; entry/hooks.py; config/loader.py with config/profiles/default.yaml and quick.yaml (effort profiles that drive every timeout the plan proposes to resize).
- SUBSYSTEM, bench/: 10 files (capability.py, fixtures.py, keys.py, llm_gate.py, prose.py, report.py, runner.py) added by the HEAD commit 8ce5b1c 'one capability can be tested without building a report'; not in the plan, not run, and its relationship to benchmarks/ (judge.py, prose_judge.py, judge_alignment.py, run_manus_bench.py, manus_comparison.md, r4_panel.js, out/bench/last.json) is unexamined.
- SUBSYSTEM, DB tables never discussed: run_slots, run_ledger, login_failures, intake_sessions, owner_moves, auth_tokens, shared_reports (15 tables total; the audit counted only jobs, accounts, entitlements, credit_spends, coupons).
- FILES, ops: backup.py has zero test mentions and --verify was never run against any file; no test parses either plist in config/launchd; docs/baselines holds 7 files (M1.json, M2.json, M3.json, deterministic_baseline.json, harness_baseline.json, wave0.json, wave2.json) while the plan speaks of one; docs/ has 61 files including AUDIT_PLAN.md, AUDIT_RESULTS.md, R4_BURNDOWN.md, R4_FIX_ORDER.md, R4_HANDOFF.md, HARNESS_STATE.md, TESTING_MILESTONES.md, none reconciled with this audit; web/dashboard.html (364), web/home.html (630), web/account.js (210) unnamed.
- TESTS: the ~500 model-written tests were never sampled for shape-only assertions; no coverage run; the 4114/9 suite figure is from an earlier run and was not repeated.

## Claims resting on reading rather than running

- KNOWN-STATE STATUS, 'a real run has NOT been exercised since the last two weeks of changes': PARTLY WRONG. Read-only DB query: 8 non-stub, owner-submitted, state=complete plan jobs since Aug 29 (Aug 29 x2, Aug 31, Sep 2, Sep 3, Sep 4, Sep 8 x2; standard effort; 363 to 1074 s each; three of them revisions via previous_job_id), the newest at 2026-09-08 17:37 UTC, one day before the last commit; plus out/live/diag01.json generated today 12:18 by tools.run_live. What has not happened is an API-path run with the stub off AND the paywall on AND a checkout; the pipeline itself has run. All 63 stub clones in the DB are also since Aug 29.
- KNOWN-STATE STATUS, env: VERIFIED. .env has CASTOR_STUB_REPORT=88b4..., CASTOR_PAYWALL_OFF=1, no SESSION_SECRET, no CASTOR_ENV, GROQ_API_KEY empty, STRIPE_WEBHOOK_SECRET and all six STRIPE_PRICE_* set, CASTOR_PUBLIC_URL set, GOOGLE_CLIENT_ID set. launchctl lists com.castor.server (pid 1047) and com.castor.tunnel (pid 1034); local and public /login both 200.
- KNOWN-STATE STATUS, 'section sometimes misses its run, suspected parallel dispatch': UNSUPPORTED BY THE DATA. Of 370 jobs, 23 carry _dropped_outputs; the recorded drops are price_intel x21 ('no plausible prices scraped'), pricing_benchmark x13 (a deliberate skip for geo-sourced local ventures), clustering x3 (fewer than 4 competitors), consumer_research x2, audiences x2, personas x2. Zero drops cite a timeout. Only 3 jobs ever contain '(product section timed out)' (Jun 8, Jun 17, Aug 28), 10 contain 'timed out', 1 contains _llm_exhaustion. Nothing in the DB points at the fan-out; the hypothesis behind RR-2, RR-3, LC-1 and LC-4 rests on the owner's recollection, and the only 11 lines of log cannot confirm it.
- KNOWN-STATE STATUS, repo hygiene: VERIFIED. feedback-for-jane.pdf/.txt tracked; git check-ignore matches neither .jobs.sqlite-wal nor .pytest_cache; .jobs.sqlite 45.9MB, -shm and a 0-byte -wal present, 0-byte jobs.db present; 355 'except Exception' in non-test, non-vendored .py files (matches the scout).
- KNOWN-STATE STATUS, SK-22 coupon push: VERIFIED LIVE. Today's boot log shows six Stripe 400s on /v1/promotion_codes followed by '[startup] pushed 6 coupon(s) minted before Stripe was configured'.
- KNOWN-STATE STATUS, SEC-1 and OPS-6: VERIFIED that http://app.castor-advisory.com/login returns 200 with no redirect and GET /readyz is 404. NOT IN THE PLAN: GET /healthz already exists and returns 200; the OPS-6 item should say why a second endpoint is needed instead of deepening /healthz.
- SK-4 'the price_intel section is absent from every corpus report' as proof of the float-on-dict bug: the DB shows two distinct causes, 21 drops from the scrape finding nothing and 13 deliberate skips for local ventures; the bug is plausible but the proof conflates it with policy, so the M2 gate 'the price scrape produces a price' may still fail after the fix on every hyperlocal case.
- OPS-2 'today the lifespan yields' with Stripe keys and the stub: read from api._refuse_to_boot_misconfigured, never entered under that env.
- M2 'GET /billing/status stays 0 today' after card 4242, and the claim that no webhook is registered on the app host: read from a dashboard screen and .env, no checkout run, no signed event replayed.
- RR-1/M3 'unrefunded today' for stale running rows: read from jobs.requeue_orphans; the 10 restart-related error rows in the DB were not checked for refunds, so the size of the money problem is unknown.
- G4/SK-5 'POST /plan with a 30-character brief is 200 and consumed today', SK-10 'every hop 200 today' on the revision chain (267 jobs carry previous_job_id, none audited for an unpaid hop), M4/SK-11 'consumed today' under PAYWALL_OFF: all read from routes/research.py:510-570; none issued against the app.
- RR-2/SK-3, CH-1/SK-20, RR-3/SK-17 'today publishable stays True / D52 abstains / reservations continue': read from plan.py and capabilities/scheduler.py; the fan-out was never driven with a stubbed timeout.
- G1 'both return the same mid today' and G3 'no finding today' on calc 1,200,000*2: read from market_sizing.py, triangulation.py and report/verifier.py; not executed on a corpus report.
- LC-5 '15.6s today' and '6.3s today', LC-3 'second stays pending today', LC-4 'serial today': timing numbers with no recorded run; they came from reading tenacity defaults and the sleep sites.
- CH-5 and UX-6 'no test fails today when gate() changes': asserted; no node harness exists to show it, and no browser executed survey.js.
- SEC-2 'every one survives today' after DELETE /auth/account: read from auth.delete_account; not run against a fixture account.
- OPS-1 gate 'backup.py --verify reports every table matching': backup.py has no tests and --verify has never been run on any file; ./backups is absent, so no backup has ever been taken from this checkout.
- Every effort estimate (2S+2M, 13S+2M, 9S+6M+1L, 5S+3M+1L) and every 'fails today' sentence in the 44 proofs is a prediction about a test that does not exist; none was written or run.
- 'The chain is Gemini alone' is inferred from GROQ_API_KEY being empty and LLM_ALLOW_PAID/LLM_BACKEND absent from .env; llm.fallback_chain() was not called to print the chain, and diag01's _cogs block, which would show which models actually answered, was not read.
- Suite 4114 passed, 9 skipped in 8m38s: from an earlier session; the suite was not re-run at 8ce5b1c.

## The one experiment to run first

Run one real, stub-off, paywall-on report through the API path on a second port against a copy of the database, with a Stripe test-mode checkout in front of it. How: cp .jobs.sqlite to the scratchpad; start `.venv/bin/python -m uvicorn api:app --port 8766` with JOBS_DB_PATH pointing at the copy, CASTOR_STUB_REPORT and CASTOR_PAYWALL_OFF unset, everything else from .env; run `stripe listen --forward-to 127.0.0.1:8766/billing/webhook` (or skip it to observe the M2 claim that the credit never arrives); the owner completes the 4242 checkout from http://127.0.0.1:8766 and submits a three-sentence brief; watch /billing/status, GET /jobs/{id}, the mail path and the withhold/refund branch; read the finished result's _dropped_outputs, _llm_exhaustion, _cogs and verification.summary. Time: about 15 minutes to set up and 10 to 20 minutes of generation (the Sep 8 real runs took 529 and 588 s at standard effort, the worst since Aug 29 took 1074 s), so under an hour wall clock, no secret rotation and no change to the live server. Why this one: every M0 and M1 item, and the serial order of the three billing edits in routes/research.py, assumes the fresh-run path still works after 31 commits in 14 days, yet the most recent real-run failure in the DB is a NameError ('checkpoint' is not defined) and the boot guard, consume-before-check, claim_run_slot, _finish, notify and refund branches have only been read. If the run boots, charges, completes and mails, M0 shrinks to the webhook and backup items and the plan's order stands; if it crashes or strands a credit, M1's billing items move ahead of M0's kill-mid-run gate and the crash goes to the top. It also yields the first stub-free API-path report that can be read for quality and the first measurement of drops under a Gemini-only chain, which decides whether RR-3/GROQ belongs in M0 rather than M1. A ten-minute read-only precursor already reshapes one item: querying the 23 _dropped_outputs rows shows the drops are price_intel, pricing_benchmark and clustering, none timeout- or dispatch-related, so 'a section sometimes misses its run, suspected parallel dispatch' should be restated as unmeasured before RR-2/LC-1/LC-4 are scoped around it.
